import type { WidgetLayout } from "@itcom/db/schema";

export default function MentorApplicationWidget({
	size: _size,
}: {
	size: WidgetLayout["size"];
}) {
	return <div>Mentor Application Widget (P2 - Coming Soon)</div>;
}
