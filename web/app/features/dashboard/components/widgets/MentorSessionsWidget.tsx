import type { WidgetLayout } from "@itcom/db/schema";

export default function MentorSessionsWidget({
	size: _size,
}: {
	size: WidgetLayout["size"];
}) {
	return <div>Mentor Sessions Widget (P2 - Coming Soon)</div>;
}
