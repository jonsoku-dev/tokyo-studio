import type { WidgetLayout } from "@itcom/db/schema";

export default function DocumentHubWidget({
	size: _size,
}: {
	size: WidgetLayout["size"];
}) {
	return <div>Document Hub Widget (P2 - Coming Soon)</div>;
}
