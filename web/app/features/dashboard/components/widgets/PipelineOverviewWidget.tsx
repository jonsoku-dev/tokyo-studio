import type { WidgetLayout } from "@itcom/db/schema";

export default function PipelineOverviewWidget({
	size: _size,
}: {
	size: WidgetLayout["size"];
}) {
	return <div>Pipeline Overview Widget (P2 - Coming Soon)</div>;
}
