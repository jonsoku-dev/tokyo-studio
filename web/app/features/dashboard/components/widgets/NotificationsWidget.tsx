import type { WidgetLayout } from "@itcom/db/schema";

export default function NotificationsWidget({
	size: _size,
}: {
	size: WidgetLayout["size"];
}) {
	return <div>Notifications Widget (P2 - Coming Soon)</div>;
}
