import type { WidgetLayout } from "@itcom/db/schema";

export default function SettlementChecklistWidget({
	size: _size,
}: {
	size: WidgetLayout["size"];
}) {
	return <div>Settlement Checklist Widget (P2 - Coming Soon)</div>;
}
