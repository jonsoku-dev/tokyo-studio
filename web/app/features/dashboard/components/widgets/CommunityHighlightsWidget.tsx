import type { WidgetLayout } from "@itcom/db/schema";

export default function CommunityHighlightsWidget({
	size: _size,
}: {
	size: WidgetLayout["size"];
}) {
	return <div>Community Highlights Widget (P2 - Coming Soon)</div>;
}
